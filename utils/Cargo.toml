[package]
name = "utils"
version = "0.1.0"
edition = "2024"

[dependencies]
pnet = { version = "0.35.0", optional=true }
pcap = { version = "2.4.0", optional=true }

[features]
default = ["log"]
log = []
net = []
pcap-utils = ["pcap"]
pnet-utils = ["pnet"]