[package]
name = "pnet-sniff"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
pnet = "0.35.0"
utils = { version = "0.1.0", path = "../utils", features = ["pnet-utils"] }
