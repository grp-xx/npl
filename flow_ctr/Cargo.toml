[package]
name = "flow_ctr"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
etherparse = "0.20.1"
pcap = "2.4.0"
utils = { version = "0.1.0", path = "../utils", features = ["log", "net"] }
